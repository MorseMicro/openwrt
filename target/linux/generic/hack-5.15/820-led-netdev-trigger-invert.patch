--- a/drivers/leds/trigger/ledtrig-netdev.c
+++ b/drivers/leds/trigger/ledtrig-netdev.c
@@ -35,6 +35,7 @@
  *         (has carrier) or not
  * tx -  LED blinks on transmitted data
  * rx -  LED blinks on receive data
+ * invert - flip blink/on logic (e.g. link up = LED off)
  *
  */
 
@@ -60,6 +61,7 @@ static void set_baseline_state(struct le
 {
 	int current_brightness;
 	struct led_classdev *led_cdev = trigger_data->led_cdev;
+	bool invert;
 
 	/* Already validated, hw control is possible with the requested mode */
 	if (trigger_data->hw_control) {
@@ -74,10 +76,13 @@ static void set_baseline_state(struct le
 	if (!led_cdev->blink_brightness)
 		led_cdev->blink_brightness = led_cdev->max_brightness;
 
+ 	invert = test_bit(TRIGGER_NETDEV_INVERT, &trigger_data->mode);
+
 	if (!trigger_data->carrier_link_up) {
-		led_set_brightness(led_cdev, LED_OFF);
+		led_set_brightness(led_cdev,
+			invert ? led_cdev->blink_brightness : LED_OFF);
 	} else {
-		if (test_bit(TRIGGER_NETDEV_LINK, &trigger_data->mode))
+		if (invert ^ test_bit(TRIGGER_NETDEV_LINK, &trigger_data->mode))
 			led_set_brightness(led_cdev,
 					   led_cdev->blink_brightness);
 		else
@@ -126,6 +131,9 @@ static bool can_hw_control(struct led_ne
 	struct led_classdev *led_cdev = trigger_data->led_cdev;
 	int ret;
 
+	if (test_bit(TRIGGER_NETDEV_INVERT, &trigger_data->mode))
+		return false;
+
 	if (!supports_hw_control(led_cdev))
 		return false;
 
@@ -236,6 +244,7 @@ static ssize_t netdev_led_attr_show(stru
 	case TRIGGER_NETDEV_LINK:
 	case TRIGGER_NETDEV_TX:
 	case TRIGGER_NETDEV_RX:
+	case TRIGGER_NETDEV_INVERT:
 		bit = attr;
 		break;
 	default:
@@ -261,6 +270,7 @@ static ssize_t netdev_led_attr_store(str
 	case TRIGGER_NETDEV_LINK:
 	case TRIGGER_NETDEV_TX:
 	case TRIGGER_NETDEV_RX:
+	case TRIGGER_NETDEV_INVERT:
 		bit = attr;
 		break;
 	default:
@@ -297,6 +307,7 @@ static ssize_t netdev_led_attr_store(str
 DEFINE_NETDEV_TRIGGER(link, TRIGGER_NETDEV_LINK);
 DEFINE_NETDEV_TRIGGER(tx, TRIGGER_NETDEV_TX);
 DEFINE_NETDEV_TRIGGER(rx, TRIGGER_NETDEV_RX);
+DEFINE_NETDEV_TRIGGER(invert, TRIGGER_NETDEV_INVERT);
 
 static ssize_t interval_show(struct device *dev,
 			     struct device_attribute *attr, char *buf)
@@ -340,6 +351,7 @@ static struct attribute *netdev_trig_att
 	&dev_attr_link.attr,
 	&dev_attr_rx.attr,
 	&dev_attr_tx.attr,
+	&dev_attr_invert.attr,
 	&dev_attr_interval.attr,
 	NULL
 };
@@ -404,11 +416,12 @@ static void netdev_trig_work(struct work
 	unsigned int new_activity;
 	struct rtnl_link_stats64 temp;
 	unsigned long interval;
-	int invert;
+	bool link;
+	bool invert;
 
-	/* If we dont have a device, insure we are off */
+	/* If we don't have a device, ensure we are in the baseline state */
 	if (!trigger_data->net_dev) {
-		led_set_brightness(trigger_data->led_cdev, LED_OFF);
+		set_baseline_state(trigger_data);
 		return;
 	}
 
@@ -427,14 +440,15 @@ static void netdev_trig_work(struct work
 	if (trigger_data->last_activity != new_activity) {
 		led_stop_software_blink(trigger_data->led_cdev);
 
-		invert = test_bit(TRIGGER_NETDEV_LINK, &trigger_data->mode);
+		invert = test_bit(TRIGGER_NETDEV_INVERT, &trigger_data->mode);
+		link = test_bit(TRIGGER_NETDEV_LINK, &trigger_data->mode);
 		interval = jiffies_to_msecs(
 				atomic_read(&trigger_data->interval));
 		/* base state is ON (link present) */
 		led_blink_set_oneshot(trigger_data->led_cdev,
 				      &interval,
 				      &interval,
-				      invert);
+				      invert ^ link);
 		trigger_data->last_activity = new_activity;
 	}
 
--- a/include/linux/leds.h
+++ b/include/linux/leds.h
@@ -530,6 +530,7 @@ enum led_trigger_netdev_modes {
 	TRIGGER_NETDEV_LINK = 0,
 	TRIGGER_NETDEV_TX,
 	TRIGGER_NETDEV_RX,
+	TRIGGER_NETDEV_INVERT,
 
 	/* Keep last */
 	__TRIGGER_NETDEV_MAX,
